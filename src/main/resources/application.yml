spring:
  application:
    name: user-management-system

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never

info:
  application:
    name: ${spring.application.name}
    description: 日本IT就職活動向けのユーザー管理・認証学習プロジェクト
